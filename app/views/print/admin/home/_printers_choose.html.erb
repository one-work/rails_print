<div class="medias as-boxed">
<% @printer_aims.each do |printer_aim| %>
  <%= button_to({ action: 'task' }, params: { printer_id: printer_aim.printer_id, **params.permit(:gid, :aim) }, class: 'media') do %>
    <div class="media-content">
      <p><%= printer_aim.printer.display_name %></p>
    </div>
    <div class="media-right">
      <%= svg_tag 'print', class: 'icon is-small is-primary' %>
      <span class="text-grey">选择打印</span>
    </div>
  <% end %>
<% end %>
</div>