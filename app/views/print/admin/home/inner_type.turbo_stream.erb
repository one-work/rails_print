<% if @printer.dev_type_esc? %>
  <%= turbo_stream.append_body partial: 'printer_to_cpcl', layout: 'modal' %>
<% else %>
  <%= turbo_stream.append_body partial: 'printer_to_esc', layout: 'modal' %>
<% end %>
