<%=
  tr_tag(
    model,
    {
      action: url_for({ action: 'resend', printer_id: model.printer_id, id: model.id }),
      icon: 'paper-plane',
      title: t('.resend.title')
    }
  ) { yield }
%>